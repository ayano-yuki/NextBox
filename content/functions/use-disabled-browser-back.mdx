---
title: ブラウザバックの禁止
description: ブラウザの戻る操作（popstate）を検知し、戻り動作をキャンセルするためのカスタムフック
---

import { DisabledBrowserBackPreview } from '$/previews/DisabledBrowserBackPreview'

# ブラウザバックの禁止

これは、**ブラウザの戻る操作（popstate）を検知し、戻り動作をキャンセルするためのカスタムフック** です。

<DisabledBrowserBackPreview />

## やっていること

<SourceFile path="@/functions/disabledBrowserBack.ts" language="typescript" />

1. 現在の URL を pushState で履歴に積む
   - popstate が確実に発火する状態を作る
2. popstate イベントを監視するリスナーを登録
   - ブラウザが「戻る」を試みた瞬間にフックされる
3. 戻ろうとしたらアラートを出し、再度 pushState で戻れないようにする
   - 実質「戻る」操作を無効化
4. コンポーネント unmount 時にリスナーを削除
   - メモリリーク防止